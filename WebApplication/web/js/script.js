let email = document.querySelector('input[name="txtEmail"]')
let peso = document.querySelector('input[name="txtpeso"]')

let form = document.querySelector('#form-calculo')

form.addEventListener('submit', function (event) {
    event.preventDefault();
    //objeto do banco
    //if do email se existente
    //se sim fazer o insert da pesagem no email e adicionar(form.submit();)
    //se não informar um alert "email não existente"
})